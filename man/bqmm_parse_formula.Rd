% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formula.R
\name{bqmm_parse_formula}
\alias{bqmm_parse_formula}
\title{Parse an lme4-style mixed-model formula}
\usage{
bqmm_parse_formula(formula, data, na.action = stats::na.omit, contrasts = NULL)
}
\arguments{
\item{formula}{A model formula such as \code{y ~ x + (1 + x | group)}.}

\item{data}{A data frame.}

\item{na.action, contrasts}{Passed through to model-frame construction.}
}
\value{
A list with elements:
\describe{
\item{y}{numeric response vector.}
\item{X}{fixed-effect design matrix (N x K).}
\item{Z}{random-effect design matrix (N x Q), dense.}
\item{sd_map}{integer vector (length Q) mapping each Z column to a
variance component in \code{1:G}.}
\item{re_components}{character labels for the \code{G} variance components.}
\item{re_terms}{per-term metadata: grouping factor, coefficient names,
number of levels.}
\item{cnms, flist, Gp}{the raw lme4 random-effect structures.}
\item{fixed_names}{column names of \code{X}.}
}
}
\description{
Thin wrapper around \code{\link[lme4:modular]{lme4::glFormula()}} that extracts everything \code{bqmm}
needs: the fixed-effect design matrix \code{X}, the random-effect design matrix
\code{Z} (dense), the response \code{y}, and a mapping from each column of \code{Z} to a
variance component. Reusing lme4's parser means nested \emph{and} crossed random
effects are handled for free.
}
\keyword{internal}
