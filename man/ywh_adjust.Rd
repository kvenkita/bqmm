% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adjust.R
\name{ywh_adjust}
\alias{ywh_adjust}
\title{Yang-Wang-He posterior-variance correction (mixed-model form)}
\usage{
ywh_adjust(object, meat = c("cluster", "independence"))
}
\arguments{
\item{object}{A fitted \code{bqmm} object.}

\item{meat}{Meat estimator: \code{"cluster"} (default; cluster-robust on the first
grouping factor) or \code{"independence"}.}
}
\value{
A list with the adjusted fixed-effect covariance \code{vcov}, the naive
posterior covariance \code{vcov_naive}, the meat \code{G}, and \code{sigma}.
}
\description{
The asymmetric Laplace likelihood is a \emph{working} (misspecified) likelihood,
so the naive MCMC posterior covariance of the fixed effects is not the
asymptotic variance of the quantile-regression estimator. Yang, Wang and He
(2016) correct this with a multiplicative sandwich that re-uses the posterior
covariance as the "bread":
}
\details{
V_adj = Sigma_post \%\emph{\% G \%}\% Sigma_post,

where \code{Sigma_post} is the posterior covariance of the fixed effects and \code{G}
is the meat (variance of the ALD working-likelihood score). For the mixed
model this is the right object to correct: \code{Sigma_post} already encodes the
multilevel pooling, so the correction retains the random-effect contribution
to fixed-effect uncertainty while fixing the misspecified ALD scale. Under
correct specification \code{G} approximately equals \code{Sigma_post^{-1}} and the
correction reduces to \code{~Sigma_post}. See \code{\link[=compute_ywh_multiplicative]{compute_ywh_multiplicative()}}.

The pure Koenker-Powell sandwich (\verb{[compute_ywh_sandwich()]}, valid for
\emph{fixed-effect} quantile regression) was found by simulation to \strong{under-cover}
the fixed effects of a mixed model, because it is computed on residuals with
the random effects removed and therefore drops the between-cluster variance.
The multiplicative form here covers at or slightly above the nominal level
across homoscedastic and heteroscedastic designs (see \code{tools/bakeoff.R}).
Validity is claimed for the fixed-effect block only; variance components keep
their model-based posterior.
}
\keyword{internal}
