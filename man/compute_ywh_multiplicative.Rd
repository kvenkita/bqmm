% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adjust.R
\name{compute_ywh_multiplicative}
\alias{compute_ywh_multiplicative}
\title{Yang-Wang-He multiplicative variance correction}
\usage{
compute_ywh_multiplicative(Sigma_post, X, resid, sigma, tau, groups = NULL)
}
\arguments{
\item{Sigma_post}{Posterior covariance of the fixed effects (K x K).}

\item{X}{Fixed-effect design matrix (N x K).}

\item{resid}{Conditional residuals \verb{y - (X beta + Z b)} at the posterior
median fit.}

\item{sigma}{Posterior ALD scale (a positive scalar).}

\item{tau}{Quantile level.}

\item{groups}{Optional integer cluster index (length N). \code{NULL} gives the
independence meat.}
}
\value{
A symmetric K x K covariance matrix.
}
\description{
Computes \code{V = Sigma_post \%*\% G \%*\% Sigma_post}, the mixed-model form of the
YWH correction (see \code{\link[=ywh_adjust]{ywh_adjust()}}). The ALD working-likelihood score for
observation i is \verb{s_i = (1/sigma) x_i (tau - 1\{resid_i < 0\})}, so the meat is
\verb{G = (1/sigma^2) sum_i x_i x_i' psi_i^2} (independence) or
\verb{G = (1/sigma^2) sum_g (sum_\{i in g\} x_i psi_i)(...)'} (cluster-robust),
with \verb{psi_i = tau - 1\{resid_i < 0\}}.
}
\keyword{internal}
