% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bqmm.R
\name{bqmm}
\alias{bqmm}
\title{Bayesian multilevel quantile regression}
\usage{
bqmm(
  formula,
  data,
  tau = 0.5,
  family = ald(),
  prior = NULL,
  cov = c("diagonal", "unstructured"),
  adjust = TRUE,
  prior_only = FALSE,
  chains = 4,
  iter = 2000,
  warmup = floor(iter/2),
  cores = getOption("mc.cores", 1L),
  seed = NULL,
  control = list(adapt_delta = 0.95),
  ...
)
}
\arguments{
\item{formula}{An lme4-style model formula.}

\item{data}{A data frame containing the variables in \code{formula}.}

\item{tau}{Quantile level(s) in (0, 1). Scalar or vector.}

\item{family}{A \code{bqmm_family} object; currently only \code{\link[=ald]{ald()}}.}

\item{prior}{A \code{\link[=bqmm_prior]{bqmm_prior()}} object, or \code{NULL} for data-scaled defaults.}

\item{cov}{Random-effect covariance structure. \code{"diagonal"} (default)
models independent random effects and supports any number of nested or
crossed terms. \code{"unstructured"} adds an LKJ-correlated covariance but
currently requires exactly one random-effects term (e.g.
\code{y ~ x + (1 + x | g)}).}

\item{adjust}{Logical; compute the Yang-Wang-He (2016) variance correction so
that \code{vcov(fit, adjusted = TRUE)} returns valid fixed-effect uncertainty.
Default \code{TRUE}.}

\item{prior_only}{Logical; sample from the prior predictive distribution.}

\item{chains, iter, warmup, cores, seed}{Passed to \code{\link[rstan:stanmodel-method-sampling]{rstan::sampling()}}.}

\item{control}{A list of sampler control parameters (e.g. \code{adapt_delta}).
Defaults raise \code{adapt_delta} to 0.95 because ALD posteriors are sharp.}

\item{...}{Additional arguments forwarded to \code{\link[rstan:stanmodel-method-sampling]{rstan::sampling()}}.}
}
\value{
A \code{bqmm} object (single \code{tau}) or a \code{bqmm_multi} object (vector
\code{tau}).
}
\description{
Fits a Bayesian mixed-effects quantile regression model using the asymmetric
Laplace working likelihood and Stan. The interface follows \code{lme4}: random
effects are written inline in the formula, e.g. \code{y ~ x + (1 + x | group)},
and nested or crossed grouping factors are both supported.
}
\details{
One or several quantiles may be requested through \code{tau}. A scalar returns a
single \code{bqmm} fit; a vector fits each quantile independently and returns a
\code{bqmm_multi} container.
}
\examples{
\dontrun{
fit <- bqmm(distance ~ age + (1 | Subject),
            data = nlme::Orthodont, tau = c(0.1, 0.5, 0.9))
summary(fit)
}
}
